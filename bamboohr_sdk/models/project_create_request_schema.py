
"""
BambooHR API

BambooHR API documentation. https://www.bamboohr.com/api/documentation/

The version of the OpenAPI document: 1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing_extensions import Self

from bamboohr_sdk.models.task_create_schema import TaskCreateSchema


class ProjectCreateRequestSchema(BaseModel):
    """
    Schema for creating a new time tracking project with optional tasks
    """

    name: StrictStr = Field(description="Name of the project.")
    billable: StrictBool | None = Field(
        default=None, description="Indicates if the project is billable. Defaults to true if not provided."
    )
    allow_all_employees: StrictBool | None = Field(
        default=None,
        description="Indicates if all employees can log time for this project. Defaults to true if not provided.",
        alias="allowAllEmployees",
    )
    employee_ids: list[StrictInt] | None = Field(
        default=None, description="A list of employee IDs that can log time for this project.", alias="employeeIds"
    )
    has_tasks: StrictBool | None = Field(
        default=None,
        description="Indicates if the project has tasks. Defaults to false if not provided.",
        alias="hasTasks",
    )
    tasks: list[TaskCreateSchema] | None = Field(
        default=None, description="List of tasks to create and associate with the project."
    )
    __properties: ClassVar[list[str]] = ["name", "billable", "allowAllEmployees", "employeeIds", "hasTasks", "tasks"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of ProjectCreateRequestSchema from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in tasks (list)
        _items = []
        if self.tasks:
            for _item_tasks in self.tasks:
                if _item_tasks:
                    _items.append(_item_tasks.to_dict())
            _dict["tasks"] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of ProjectCreateRequestSchema from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "name": obj.get("name"),
                "billable": obj.get("billable"),
                "allowAllEmployees": obj.get("allowAllEmployees"),
                "employeeIds": obj.get("employeeIds"),
                "hasTasks": obj.get("hasTasks"),
                "tasks": [TaskCreateSchema.from_dict(_item) for _item in obj["tasks"]]
                if obj.get("tasks") is not None
                else None,
            }
        )
        return _obj
