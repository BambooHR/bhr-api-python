
"""
BambooHR API

BambooHR API documentation. https://www.bamboohr.com/api/documentation/

The version of the OpenAPI document: 1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from typing import Any, ClassVar

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing_extensions import Self

from bamboohr_sdk.models.webhook_event_type import WebhookEventType


class NewWebHook(BaseModel):
    """
    NewWebHook
    """

    name: StrictStr = Field(description="The name of the webhook.")
    monitor_fields: list[StrictStr] = Field(description="A list of fields to monitor.", alias="monitorFields")
    post_fields: dict[str, Any] | None = Field(
        default=None,
        description="A list of fields to post to the webhook url. Field ID or alias: external name",
        alias="postFields",
    )
    url: StrictStr = Field(description="The url the webhook should send data to (must begin with https://).")
    format: StrictStr | None = Field(
        default=None, description="The format the webhook should use (json - default, form-encoded)."
    )
    include_company_domain: StrictBool | None = Field(
        default=None,
        description="If set to true, the company domain will be added to the header.",
        alias="includeCompanyDomain",
    )
    events: list[WebhookEventType] | None = Field(
        default=None,
        description="Events that trigger this webhook. Defaults to ['employee_with_fields.updated', 'employee_with_fields.deleted', 'employee_with_fields.created'] if not specified. Cannot mix employee_with_fields events with employee events.",
    )
    __properties: ClassVar[list[str]] = [
        "name",
        "monitorFields",
        "postFields",
        "url",
        "format",
        "includeCompanyDomain",
        "events",
    ]

    @field_validator("format")
    def format_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["json", "form-encoded"]):
            raise ValueError("must be one of enum values ('json', 'form-encoded')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of NewWebHook from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of NewWebHook from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "name": obj.get("name"),
                "monitorFields": obj.get("monitorFields"),
                "postFields": obj.get("postFields"),
                "url": obj.get("url"),
                "format": obj.get("format"),
                "includeCompanyDomain": obj.get("includeCompanyDomain"),
                "events": obj.get("events"),
            }
        )
        return _obj
