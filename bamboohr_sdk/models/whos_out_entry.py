"""
BambooHR API

BambooHR API documentation. https://www.bamboohr.com/api/documentation/

The version of the OpenAPI document: 1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import date
from typing import Any, ClassVar

from pydantic import BaseModel, ConfigDict, Field, StrictInt, StrictStr, field_validator
from typing_extensions import Self


class WhosOutEntry(BaseModel):
    """
    WhosOutEntry
    """

    id: StrictInt | None = Field(default=None, description="The time off request ID or holiday ID.")
    type: StrictStr | None = Field(
        default=None,
        description="Whether this entry is a time off request or a company holiday. Entries have two shapes depending on `type`: `timeOff` entries include `employeeId` and `name` (the employee's name); `holiday` entries include only `name` (the holiday name) and omit `employeeId`. Always check `type` before accessing `employeeId`.",
    )
    employee_id: StrictInt | None = Field(
        default=None,
        description="The employee ID. Only present for `timeOff` entries; omitted for `holiday` entries.",
        alias="employeeId",
    )
    name: StrictStr | None = Field(
        default=None,
        description="For `timeOff` entries, the employee's name. For `holiday` entries, the holiday's name.",
    )
    start: date | None = Field(default=None, description="Start date in YYYY-MM-DD format.")
    end: date | None = Field(default=None, description="End date in YYYY-MM-DD format.")
    __properties: ClassVar[list[str]] = ["id", "type", "employeeId", "name", "start", "end"]

    @field_validator("type")
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["timeOff", "holiday"]):
            raise ValueError("must be one of enum values ('timeOff', 'holiday')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of WhosOutEntry from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of WhosOutEntry from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "id": obj.get("id"),
                "type": obj.get("type"),
                "employeeId": obj.get("employeeId"),
                "name": obj.get("name"),
                "start": obj.get("start"),
                "end": obj.get("end"),
            }
        )
        return _obj
