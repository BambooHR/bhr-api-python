"""
BambooHR API

BambooHR API documentation. https://www.bamboohr.com/api/documentation/

The version of the OpenAPI document: 1.0
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""

from __future__ import annotations

import json
import pprint
import re  # noqa: F401
from datetime import date
from typing import Any, ClassVar

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing_extensions import Self


class TimeOffHistory(BaseModel):
    """
    TimeOffHistory
    """

    var_date: date = Field(description="The date for the history item in YYYY-MM-DD format.", alias="date")
    event_type: StrictStr | None = Field(
        default=None,
        description="The type of history event. Defaults to `used` for the /history path and `override` for the /balance_adjustment path when omitted.",
        alias="eventType",
    )
    time_off_request_id: StrictInt | None = Field(
        default=None,
        description="The ID of an approved time off request. Required when eventType is `used`.",
        alias="timeOffRequestId",
    )
    time_off_type_id: StrictInt | None = Field(
        default=None,
        description="The ID of the time off type. Required when eventType is `override`.",
        alias="timeOffTypeId",
    )
    amount: StrictFloat | StrictInt | None = Field(
        default=None, description="The number of hours/days to record. Required when eventType is `override`."
    )
    note: StrictStr | None = Field(default=None, description="An optional note to show in history.")
    __properties: ClassVar[list[str]] = ["date", "eventType", "timeOffRequestId", "timeOffTypeId", "amount", "note"]

    @field_validator("event_type")
    def event_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(["used", "override"]):
            raise ValueError("must be one of enum values ('used', 'override')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Self | None:
        """Create an instance of TimeOffHistory from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: set[str] = set([])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: dict[str, Any] | None) -> Self | None:
        """Create an instance of TimeOffHistory from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate(
            {
                "date": obj.get("date"),
                "eventType": obj.get("eventType"),
                "timeOffRequestId": obj.get("timeOffRequestId"),
                "timeOffTypeId": obj.get("timeOffTypeId"),
                "amount": obj.get("amount"),
                "note": obj.get("note"),
            }
        )
        return _obj
